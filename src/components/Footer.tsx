import { type NextComponentType } from "next";
import Link from "next/link";

const Footer: NextComponentType = () => {
  return (
    <div className="h-auto bg-gradient-to-r from-cyan-500 to-blue-500 px-40">
      Footer
    </div>
  );
};

export default Footer;
